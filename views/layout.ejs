<!DOCTYPE html>
<html>
  <head>
    <title>Mt. Zion UCC</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>

    <header>

      <%- partial('toolbar') %>

      <a href="/<%= ctx.maybeRef ? '?ref=' + ctx.maybeRef : '' %>">
        <h1>Mt. Zion UCC</h1>
      </a>
    </header>

    <%-body -%>

    <footer>
      <% if(!ctx.oauth().hasPrivilegedAccess) { %>
          <hr><a href="/signin">Sign in to preview changes</a>
      <% } %>
    </footer>

  </body>
</html>
